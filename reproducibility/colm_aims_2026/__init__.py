"""COLM AIMS 2026 camera-ready evidence contract, v2 (frozen-contract
successor).

Strict schema-versioned ten-cell constructed-reference profile (5 references
x 2 calibrations), canonical FINITE_STOP/NEVER_STOPPED event representation,
sentinel-coded all-pair headline estimand, the exact D7(b) regenerated
inference procedure, claim ledger + rights inventory, the two-mode
fail-closed verifier (``source`` ceiling PASS_SOURCE_ONLY; ``release``
requires independently anchored expectations and canonical run selection),
the CAMERA_READY_CLOSURE gate, and the QA-012 inventory detector.

Spec: .correctless/specs/camera-ready-aims-evidence-2.md
CLI: ``python -m reproducibility.colm_aims_2026.verify`` (see README.md).
"""
