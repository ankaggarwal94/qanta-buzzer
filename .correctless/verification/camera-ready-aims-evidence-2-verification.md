# Verification Report — camera-ready-aims-evidence-v2

## Metadata

- **Branch**: `feature/camera-ready-aims-v2`
- **HEAD**: `f0645b23bf5182748eb6671ff44d87483f0b1215` (TDD `bd93331e` + simplify `f0645b23`)
- **Date**: 2026-08-22
- **Spec**: `.correctless/specs/camera-ready-aims-evidence-2.md`
  SHA-256 `49a580040a8b53c83a012ef25942ed102811dd7396ec55c1e018867567f79374`
- **Active rules**: 71 (R-001..R-072 minus superseded R-007; 38 carried/amended v1 + 33 new v2)
- **Suite counts**: full suite 2,311 passed / 4 pre-existing skips / exit 0 (orchestrator-verified post-simplify). Independently re-verified by this step: v2 subset **605 passed / 0 failed** (65.8 s, canonical `.venv`, Python 3.11.15, NumPy 2.4.6, pytest 9.0.3); spot-runs R-052 (16), R-059 (35), R-064 (17), R-069 (12), R-027/R-017 (8) — all green.
- **Verifier method**: rule-by-rule mapping of spec → implementing sites → encoding tests (implementation and tests are rule-ID-annotated throughout); AST scans for assertion-free tests, revision-token uniqueness, and parse-site routing; import/dependency scan; `git diff main...HEAD` on protected legacy paths (empty).

## Rule Coverage

Path legend — implementation (all under `reproducibility/colm_aims_2026/`): S=schema.py, P=pairing.py, V=verifier.py, L=ledger.py, C=closure.py, Q=qa012.py, RC=receipt.py, RD=render.py, LG=legacy.py, VF=verify.py. Tests (all under `tests/`): E=test_colm_aims_v2_events_estimands.py, G=…grid_records.py, IB=…inference_block.py, D7=…inference_d7b.py, LR=…ledger_rights.py, RCC=…receipts_closure.py, SP=…schema_profile.py, SL=…sidecars_legacy.py, VR=…verifier_release.py, VS=…verifier_source_cli.py, VI=…versioning_ingress.py, H=_colm_aims_v2_helpers.py.

Strength: **strong** = violation-encoding negative fixture(s) + positive/nearest-true control; **adequate** = real but partial; **weak** = incidental only; **GAP** = none.

| Rule | Implemented (site) | Tested (would fail on violation) | Strength |
|---|---|---|---|
| R-001 | S:96–106 pinned semantic block; S:953–975 `_validate_semantic_block`; S:1078–1126 calibration-identity map (scalar rejected); S:1356–1417 `validate_profile` closed top keys | SP `TestStrictProfile` 119–196 (missing/altered/renamed/unknown per field; calibration map exact keys; `format:"QA"` never substitutes) | strong |
| R-002 | S:70–79 reserved observed ID + rejection in `validate_profile` | SP `TestObservedProfileFirewall` 203–233 (id, semantics, combined; sanctioned token pinned) | strong |
| R-003 | S:123–149 arm fields + per-family closed stop vocab; S:1009–1057 `_validate_arm` | SP `TestArms` 257–320 (missing field, all-K-way-with-idealized, cross-family confusion, unknown family, per-family accept) | strong |
| R-004 | S:620–648 `encode_json`/`encode_profile`/`decode_profile` (`allow_nan=False`) | SP `TestRoundtrip` 327–346 (equal-value roundtrip; NaN/Inf rejected at write) | strong |
| R-005 | P:155–209 `recompute_counts`/`check_count_identities`; V:1067–1108 per-cell leg | G `TestCountIdentities` 363–388 (independent oracle; ±1 mutation of every recorded count) | strong |
| R-006 | P:211–231 `compute_rates` (null at zero); S:52–58 `EmptyEvaluationError`; V:653–667, 1110–1152 | G `TestRates` 393–439 (null-at-zero, tolerance mutation, empty population = typed error) | strong |
| R-008 | P:118–153 classification + duplicate guard; P:396–410 pinned key derivation (S:108–114) | G `TestItemKeySetEquality` 197–285 + `TestExclusionBookkeeping` 509–533 (duplicates, Unicode NFC near-dupes, UNKNOWN_NOT_INFERRED, secondary diagnostics) | strong |
| R-009 | P:155–231, 245–312 (functions whose symmetry is proven) | G `TestArmReversal` 468–504 (property-based over seeded generated record sets; antisymmetric-flip vs absolute) | strong |
| R-010 | P:245–271 headline estimator; P:273–312 finite-only summary | E `TestHistoricalParity` 404–447 vs golden literals E:378–402 (produced by pinned historical impl `scripts/stopdff_v5/bootstrap.py`, generated once under NumPy 2.4.6, carried verbatim — never run-twice; second pure-python oracle) | strong¹ |
| R-011 | P:421–456 `estimand_digest`/`check_comparable`; S:214–340 closed 7B/M3 vocabularies + reconciliation; S:1223–1242; V:1200–1241 production-wired comparability leg | E `TestEstimandDigest` 464–566 (every digest field flips digest; closed vocabularies; reconciliation; recorded-digest mismatch; **production-wiring test enumerates real callers**) | strong |
| R-012 | V:89–107 leg registry; V:2000–2101 `_release_legs`; V:2269–2591 admissibility validators + `_binding_legs`; V:3134–3241 ledger legs + `_recompute_row_status` 3242+ | VR `TestBindingAdmissibility` 350–385 (mutable revision forms, UNRESOLVED, dirty), `TestMutationWiring` 285–344 (collect-don't-halt, remediation classes), LR:387 stale-PASS recompute | strong |
| R-013 | V:2103–2231 `_anchor_legs` (string-exact, no checkout); S:458–466 `resolves_inside` (symlink-free) | VR `TestIndependentAnchoring` 67–156 (in-tree/symlinked expectations refused; anchor leg without git; closed anchor block) | strong |
| R-014 | V:382–403 `classify_certifiability`; LG:31–97 enumerated families + certify privilege | SL `TestLegacyFamilies`/`TestCertifiability`/`TestV1ProfileCertifyPrivilege` 125–236 — **captured repo bytes** (`paper_exports/{csli,calibration,audit_card}.json`), aggregate-only cannot certify, dirty/superseded/unresolved → HISTORICAL_NONCERTIFYING, byte-identical input tree | strong |
| R-015 | V:1390–1536 inference recompute leg; S:328–340 interval identity fields | IB `TestIntervals` 223–275 (missing identity → non-certifying; headline binds all-pair population) | strong |
| R-016 | S:1428–1486 `publish_evidence_package` via `scripts/stopdff_v5/fileio.py`; RC:35–68 create-once receipts | SP `TestCreateOncePublish` 630–688 (second-publish fails, traversal rejected, kill-mid-publish + retry = exactly one artifact), VS:320 receipt create-once | strong |
| R-017 | V:53–88 closed verdict enums; RD:23–79 check list + RD:82–133 not-certified statement | VS `TestSourceFloor` 83–114 (enum closed, no release token, minimum positive check set, not-certified text) | strong |
| R-018 | namespace layout (all new code under `reproducibility/colm_aims_2026/` + tests/docs) | VS `TestNamespaceContainment` 365–403 (containment; legacy verifier byte-identical; fileio consumed-not-forked AST). Independent: `git diff main...HEAD -- scripts/verify_audit_release.py scripts/stopdff_v5/fileio.py` = empty | strong |
| R-019 | corpus distributed across all 11 test modules; canonical baseline `build_package_v2` (H:1222) is the shared nearest-true control | all 13 v1 classes + all listed v2 classes traced to concrete named tests (observed-as-constructed SP:208; denominator G:393; timeout-reconcile E:529; hash/model/split VR:287; historical substitution VI:169; empty-eval G:418; unbound calibration SL:189; rights LR:361; stale-PASS LR:387; zero-artifact VR:442; empty ledger VR:461; empty manifest VR:422; oversized tolerance SP:394; v2 classes per rows above/below) | adequate² |
| R-020 | S:649–795 bounded symlink-free reads + `load_records_bytes`/`load_artifact_bytes` (file+field naming, no key-dropping) | VI `TestTypedIngress` 479–514 (malformed/unknown-key/UTF-8/truncated-line name file+field), VS:426 QA2-001 deep-nesting typed | strong |
| R-021 | test-side contract; CLI runner H:1565–1626 (subprocess, documented `-m` form) | VR `TestMutationWiring` 219–344: parameterized one-at-a-time expectation mutations incl. every grid pin + inference pins (seed, index digest), verdict flip + exit codes + sentinel-free output asserted | strong |
| R-022 | VF:65–129 `allow_abbrev=False`, unknown flag/key = usage error; no disable door | VS `TestFailClosedFlags` 250–280 (unknown flag, abbreviation, gate-disable door probe, missing args) | strong |
| R-023 | L:29–136 closed enums + row fields; L:229–248 Holm row; L:250–348 `_validate_row` incl. D3 blocking-task discipline; L:404–407 anchored commit | LR `TestLedgerRows` 66–143 (per-field missing, closed status, Random-K blocking-task golden text, Holm rejected IDs must match package inference) | strong |
| R-024 | L:140–168, 308–341 EXTERNAL immunity + human attribution + venue rows; V:3195–3207 recompute exemption | LR `TestExternalRows` 149–196 (attribution required/accepted; byte-identical across tool runs; release-recompute exempt) | strong |
| R-025 | L:55–58, 116–127, 170–227 joint Random-K gates + INACTIVE discriminated multidraw protocol; V:2905–2926 authorized draws; V:2927–2988 estimand reconciliation | LR `TestRandomK` 203–296 (closed 2-member enum, headline⇔disposition joint gate, full protocol block, rng_pinned=false explicit, both draw identities, dagger, substituted-draw digest refusal) | strong |
| R-026 | L:45–53, 427–491 rights enum + terms basis + release coverage; S:653–663 opaque relative naming | LR `TestRights` 344–393 + VS `TestSentinelLeak` 170–191 + QA2-002/MA2-001 path-scrub tests (error paths never emit restricted content or absolute paths, incl. receipts) | strong |
| R-027 | RD:82–133 qualified scope lines (insensitivity narrowing; no upgrade) | VS `TestVocabulary` 135–163 over source/release/failing CLI output + direct `render_summary`; enumerated `BANNED_PHRASES`(+case-sensitive ACM terms) + `REQUIRED_QUALIFIER` H:192–213; sanctioned observed-claim token SP:229 | strong |
| R-028 | guard: H:272–307 autouse `colm_no_network`, imported by **all 11** v2 modules; deny-list scan test | VS:61 guard self-test fires; SP `TestImportScans` 605–623 (deny-list incl. urllib.request; NumPy required) | strong³ |
| R-029 | S:116–121 axes; S:977–1007 `_validate_llm_involvement` | SP `TestLlmInvolvement` 352–383 (explicit none; absent axis rejected; non-none needs tool note) | strong |
| R-030 | L:135–138 DOI pattern; L:350–366, 417–424 | LR `TestArchivalDoi` 302–337 (no-DOI rejected; GitHub URL does not qualify; optional when absent) | strong |
| R-031 | S:342–390 enumerated record field set; S:860–935 `validate_record` | SP `TestRecordAllowlist` 423–460 (free text rejected; enums closed; v2 event fields allowed) | strong |
| R-032 | S:81–82 pinned max; S:423–430, 941–951 | SP `TestTolerance` 390–417 (above-max rejected; at-max accepted; digest field per R-011) | strong |
| R-033 | V:45–47, run_verifier empty-tree typed error (V:1637), ≥1 artifact / ≥1 row / ≥1 manifest entry legs (V:3070, 3171) | VR `TestVacuousInputs` 441–472 + VS:409 source-side (path+layout named; zero rows/artifacts fail) | strong |
| R-034 | JSON/JSONL-only ingress; matrix regenerated in-memory P:477–506 | SP:610 AST/import scan (pickle/marshal/torch.load/yaml absent); D7:150 regeneration determinism | strong |
| R-035 | V:3041–3097 `_manifest_legs` (both directions + allowlist); V:753–815 records↔grid bijection | VR `TestManifestReconciliation` 391–434 + G `TestRecordFileBijection` 147–191 | strong |
| R-036 | RC:29–68 `emit_receipt` (schema-versioned, outside tree, create-once, run-scoped unique) | VS `TestReceipts` 286–357 (every run incl. failing; distinct receipts; create-once; in-tree refused; R-058 constants) | strong |
| R-037 | VF:22–33 repo-root-first bootstrap; VF:35–39 exit codes 0–4; VF:214 module-run | VS `TestCliContract` 198–244 (exit 0/1/2/3 end-to-end; direct-path bootstrap from any cwd); exit-3-never-4 negatives VI:344, VS:426. Exit 4 is constant-pinned but has **no positive end-to-end trigger fixture** | adequate⁴ |
| R-038 | `reproducibility/colm_aims_2026/README.md` (76 lines: modes, layout, verdicts, exit codes, receipts, disambiguation); 6-line qualification headers added to the seven targets (TDD commit stat) | RCC `TestDocAudit` 336–372 (README CLI contract; parametrized seven-target qualification; source_to_claim historical header; no doc redefines legacy verifier); ledger records the doc (H:1079) | strong |
| R-039 | V:470–522 `resolve_canonical_package`; publish run-scoped (S:1428) | VR `TestCanonicalSelection`:594 unit pointer contract; :610 retired-run bytes retained unchanged | strong |
| R-040 | S:191–198 frozen grid constants; S:1128–1162 `_validate_grid_block`; V:681–751 exact-set leg | G `TestGridCompleteness` 59–141 (missing/extra/renamed member, missing/duplicated/extra cell, **subset-not-enough**) | strong |
| R-041 | V:753–815 `_record_file_bijection_leg` (both directions + orphans + shared-file) | G `TestRecordFileBijection` 147–191 (declared-absent, present-undeclared, orphaned mapping, two-cells-one-file) | strong |
| R-042 | S:197 `EXPECTED_COMPLETE_PAIRS=2249`; V:816–868 per-cell count + cross-cell byte-exact set equality | G `TestItemKeySetEquality` 197–285 (±1, one-key-swapped-same-count, in-package exclusion breaks gate by construction) | strong |
| R-043 | V:870–906 held-fixed identities; V:908–959 within-calibration-only MC-stop equality | G `TestHeldFixedIdentities` 291–345 (within-calibration mismatch FAILs; **nearest-true cross-calibration difference PASSes**); D7:428–445 fixture properties | strong |
| R-044 | V:120–127 anchored grid pins; V:2593–2632 `_anchored_grid_legs` (field-by-field semantic comparison, never package hash) | VR:123 missing expectations grid block fails closed; VR:309 item-keys pin leg fires; every grid pin mutated in `TestMutationWiring` | strong |
| R-045 | S:151–157 closed enums; S:800–858 `_validate_arm_event` (every combination); V:961–1005 record-set bound identities | E `TestEventCombinations` 46–117 (FINITE/null, missing stop, NEVER/numeric, absent≠null, imputation marker, missing bound identities) | strong |
| R-046 | S:171–172 pinned producer convention; P:233–243 derived scalar (never overwrites); S:838–858 overwrite-signature rejection | E `TestFinalPrefixCrossing` 123–168 (crossing mislabeled FAILs; distinct fields roundtrip; convention token closed) | strong |
| R-047 | S:159–169 `AMBIGUOUS_TERMINAL_SENTINEL` enum member; P:318–390 `normalize_legacy_terminal` | E `TestAmbiguousLegacySentinel` 175–221 (refuses without authentication; crossing flag → FINITE; authenticated convention honored) | strong |
| R-048 | P:245–271 headline estimator; S:182–189 closed labels; V:1029–1034, 1154–1199 | E `TestHeadlineEstimand` 227–278 (population must be all-pairs, label closed, n=2249, tiny-records recompute, sign convention D7:469) | strong |
| R-049 | P:273–312 finite-only summary (own denominator/label) | E `TestEstimandSeparation` 284–334 (conditional population declared; n=n_both_finite; distinct digests; cannot pool) | strong |
| R-050 | P:411–419 canonical UTF-8 order + order digest; P:580–601 shift vector; V:1345–1355 | IB `TestCanonicalVectors` 48–68 (permuted order digest FAILs; mutated mean FAILs); D7 hand-checked recomputes | strong |
| R-051 | S:198, 207–212 pinned plan tokens; P:484–506 one shared matrix; V:1314–1342 exact-match plan + **runtime NumPy==2.4.6 leg** | IB `TestResamplePlanFields` 74–103 (each plan field, per-cell matrix rejected); D7 `TestResampleMatrix` 127–206 (endpoint/dtype/bitgen/seed change bytes; golden SHA); D7:51 NumPy pin | strong |
| R-052 | S:200–205 exact derivation string; P:464–475 `d7b_seed`; S:1100–1126 zero in-package exclusions + upstream-unpaired in provenance; V:1253–1312 triple bind; V:2633+ release re-derivation from expectations digest | IB `TestSeedBinding` 109–181 (wrong/extra seed, missing derivation, nonempty excluded_keys, keyset↔records mismatch, bool, >uint64, retention docs); D7 `TestSeedDerivation` 66–121 (goldens, domain-string, determinism); VR:301 anchored seed leg. **Spot-run: 16 passed** | strong |
| R-053 | P:508–520 digest record (4 covering fields); V:1314–1386 regenerate + compare | IB `TestMatrixDigest` 187–216 (wrong sha, missing covering field, wrong shape); D7:157 covering fields | strong |
| R-054 | P:522–527 uncentered percentile (2.5/97.5, linear); S:177–180 closed population enum; V:1390–1534 unrounded-endpoint + population gates | IB `TestIntervals` 223–263 (rounded endpoint, both_finite under headline, enum, quantile method); D7 `TestPercentileInterval` 212–252 (hand-checked) | strong |
| R-055 | P:529–538 null-centered bootstrap p with +1/1001 | IB `TestPValues` 281–296 (uncorrected x/1000 FAILs off-by-one); D7 `TestPValue` 258–297 (hand-checked; zero impossible; two-sided; ≥) | strong |
| R-056 | P:540–578 `d7b_holm` (m=10, α=.05, UTF-8 ties); V:1538–1607 stored-field recompute; L:229–248 ledger rejected IDs | IB `TestHolmFamily` 303–373 (m=8, omitted Random-K, tie order, alpha, each stored field mutated); D7 `TestHolm` 317–399 (hand-checked family, step-down, monotone capped) | strong |
| R-057 | S:174–175 exact token; S:1164–1173 discriminator required; L:246 row-side | SP `TestAnalysisProvenance` 466–485 (missing/wrong token); banned "recovered/original analysis" phrasings in vocabulary fixture (H:203–206) | strong |
| R-058 | S:61–68 the ONE constant set (`…:r2`); no `NAMESPACE_REVISION` anywhere | SP `TestRevisionConstants` 44–112 (exact set; real-int; deleted-or-alias check; **AST: no second literal revision token in namespace; assigned exactly once**) | strong |
| R-059 | S:475–510 `check_schema_version` (bool-safe `type(v) is int`, names observed/range/`VERIFIER_REVISION`); call sites: profile, ledger L:375, expectations V:524–618, manifest V:606, receipts | VI `TestVersionMatrix` 61–150: `true/1.0/"2"/3` × 5 surfaces, absent-version, typed subclass, **version-first precedence over co-present content defect**, container-shape-first, CLI exit 3. **Spot-run: 35 passed** | strong |
| R-060 | LG:47–84 `load_legacy_v1_document` (the ONE named loader, OQ-V2-002 name adopted); strict loaders refuse v1 via R-059 | VI `TestV1Transition` 167–201 (v1 refused on every strict surface; legacy loader accepts but never certifies; **AST: no strict-path import of legacy**) | strong |
| R-061 | S:393–431 typed predicates (`is_real_int` bool-safe, `is_uint64`); S:860–935 record domains; S:1164+ inference domains; grid cardinalities S:191–198 | VI `TestIntegerDomains` 207–291 (both sides of every bound; bools rejected per field; draw_count 1000; version exactly 2); E:110 bool stop_step | strong |
| R-062 | S:90–94 100-digit token guard constant; S:534–558 `_length_guarded_parse_int` (lexical, pre-`int()`); S:560–587 wired into every parse | VI `TestOverlongTokenGuard` 324–376 (150-digit typed at every site; 4400-digit never bare ValueError; CLI exit 3 never 4; legit uint64 + beyond-float sidecar int parse; guard constant = 100) | strong |
| R-063 | S:1059–1076 `_validate_closed_map`; V:109–118 CLOSED anchor block (every key required); no defaulted `dict.get` on trusted keys | SP `TestClosedMaps` 515–571 (unknown key per trusted block; **AST flags defaulted get on trusted keys**); VR:136–156 anchor typo'd/missing key = typed config error | strong |
| R-064 | V:405–464 `_classify_tree_sidecars` (object→tolerated-unknown; non-object→typed/leg); V:1691–1704 release ingress legs; LG:34,63 loader-side | SL `TestNonObjectSidecarMatrix` 55–121: `[]`,`"string"`,`0`,`true`,`null` × BOTH modes end-to-end, nearest-true unknown-object tolerated in both, malformed/UTF-8/non-finite/overlong remain defects, collect-don't-halt. **Spot-run: 17 passed** | strong |
| R-065 | L:404–407 `anchored_source_commit` required; V:2202–2225 equality leg naming both commits | VR `TestLedgerAnchorEquality` 162–186 (re-anchored ledger **with rebuilt digest** still fails) | strong |
| R-066 | V:620–651 `_git_object_exists` (env-scrubbed, `GIT_TERMINAL_PROMPT=0`, 10 s timeout); V:2173–2200 fail-closed leg (cannot-run = FAIL in release) | VR `TestGitObjectLeg` 193–216 (git disappeared → release FAIL, source unaffected; repo present + object missing → FAIL); VR:107 string-exact leg independent of checkout | strong |
| R-067 | S:432–442 `is_native_finite_number` (pre-float gate); S:512–533 three hardened hooks; V:1496–1512 ci = 2 native finite ordered numbers | VI `TestNativeFiniteInterval` 383–473 (str/bool/NaN/Inf/1e999 rejected per site; records too; **AST enumerates every `json.loads` parse site routes through the hardened loader**) | strong |
| R-068 | V:1154–1199 `_estimand_label_leg` (validated recompute identities); V:3098–3108 truth-set from schema constants, never recorded strings | E `TestEstimandSeparation`:305 label-swap FAILs; :321 F7A trusted-label truth-set regression shape (RED fixture) | strong |
| R-069 | V:470–522 `resolve_canonical_package`; V:1896–1977 `run_release_over_runs_root` (**the actual release entry**); VF `--runs-root/--ledger-pointer` flags | VR `TestCanonicalSelection` 478–592: symlinked run dir (even in-root), escape, empty relic, dangling, decoy-newer present (never newest-wins), no fallback after invalid pointer, missing pointer, refusals surface as release-run leg FAIL, flags mutually exclusive — all end-to-end through the release CLI. **Spot-run: 12 passed** | strong |
| R-070 | RC:71–163 `validate_suite_receipt` (hash-typed env digest, workflow hash, interpreter realpath, commit+tree, dirty bool, exact argv, exit code, JUnit path, skip identities, artifact hashes) | RCC `TestSuiteReceipt` 51–110 incl. the **A′-F4 defect fixture** (`environment_digest` object rejected; must be 64-hex) | strong⁵ |
| R-071 | C:1–124 `evaluate_closure` (distinct token; frozen D6 complete-closure inventory incl. `FINAL_CHECKSUMS.sha256` entries; Holm row satisfied only by `d7b_regenerated_2026`; EXTERNAL rows never block) | RCC `TestClosureGate` 117–196 (token distinct; D6 constants pinned; satisfied-except-Holm FAILs; wrong provenance token FAILs; main.tex-only binding FAILs; QA-012 unverified blocks; hash drift; unsatisfied display row) | strong |
| R-072 | Q:20–100 recursive `format:"QA"` detector (RFC 6901 pointers) + inventory manifest; C:33–38, 110–119 blocking wire-in | RCC `TestQa012Detector` 202–244 (nested/array/top-level hits, exact-value/exact-key, escaping, non-string not a hit) + `TestQa012Inventory` 246–318 (vacuous-with-hash, hit pointers+bytes hash, deterministic manifest hash, strict-parse typed, JSONL scanned, hit cannot substitute for semantic block) | strong |

**Totals: 69 strong · 2 adequate (R-019, R-037) · 0 weak · 0 GAP.**

Footnotes:
1. R-010: goldens were generated once from the pinned historical implementation under NumPy 2.4.6 at v1 RED fixture creation and carried verbatim into the reimplemented v2 suite (comment at E:377–381). This satisfies the rule's substance (pinned-historical origin, never run-twice, never hand-edited, cross-checked by an independent pure-python oracle); noted because the spec phrase "at reconstruction" could be read as requiring a fresh regeneration event during v2.
2. R-019 adequate: every named corpus class (13 v1 + all v2 additions) traces to a concrete negative test with the shared canonical package as nearest-true control, but there is no single machine-checked corpus enumeration — pairing completeness was verified by manual audit in this step.
3. R-028 placement: the no-network guard is an autouse fixture in `tests/_colm_aims_v2_helpers.py:272–307` imported by all 11 v2 modules (verified per-module), rather than a `conftest.py` — functionally equivalent and keeps the feature out of the shared repo conftest (R-018 containment); the guard has a positive self-test (VS:61).
4. R-037 residual: exit code 4 (internal error) is constant-pinned and negatively tested ("exit 3 never 4" at VI:344, VS:426–439), but no fixture positively triggers an internal error end-to-end; deliberately hard to construct once ingress is fully typed (QA/MA rounds converted the known exit-4 crashers into typed exits).
5. R-070: the validation mechanism and defect fixture are fully covered; actually attaching a receipt to the successor PR's CI suite claim is a Phase-5/6 process duty discharged at PR time, not testable here.

## Undocumented Dependencies

- **Imports** (whole namespace): stdlib (`argparse, dataclasses, datetime, errno, functools, hashlib, json, math, os, pathlib, re, stat, subprocess, sys, time, typing, unicodedata, uuid`) + **numpy** + repo-own (`scripts.stopdff_v5.fileio`, intra-namespace). No third-party additions.
- **Network-capable: none** (R-028 holds). The deny-list import scan is itself a green test (SP:610). `subprocess` is used solely for local `git cat-file -e` in the R-066 leg (V:634–643) — env-denylisted, `GIT_TERMINAL_PROMPT=0`, `stdin=DEVNULL`, 10 s timeout; no network path.
- **FINDING (manifest drift)**: `requirements.txt:8` pins `numpy==2.4.4`, but the feature hard-requires **NumPy exactly 2.4.6** (D5/R-051) — enforced at runtime by the release inference leg (V:1342 fails on version mismatch) and by the suite (`D7::test_numpy_version_exactly_2_4_6`; the v2 subset cannot pass under 2.4.4). `requirements-min.txt` (`numpy>=1.24,<3`) admits 2.4.6; the canonical `.venv` already runs 2.4.6. Non-blocking for verification (the contract is enforced in code+tests), but `requirements.txt` should be bumped to `numpy==2.4.6` — flag for /cdocs / the PR.

## Architecture Adherence

- `.correctless/ARCHITECTURE.md` (this branch, 62 lines) contains one pattern entry, **PAT-001** (likelihood factory dispatch; enforced at `models/likelihoods.py`, `qb_env/tossup_env.py`) — paths do **not** intersect the feature namespace; no ABS-xxx or TB-xxx entries exist on this branch. The v1 colm_aims architecture rows exist only on `feature/camera-ready-aims-spec` (this branch was cut from canonical `main`), so there are no stale v1 rows here to classify. /cupdate-arch will need to add the v2 namespace entries (verifier trust boundary, create-once publish, hardened-ingress pattern, single version checker).
- Conventions adhered: create-once/content-addressed publish **consumes** `scripts/stopdff_v5/fileio.py` (import verified; consumed-not-forked AST test VS:378); `scripts/verify_audit_release.py` and `scripts/stopdff_v5/fileio.py` are byte-identical to `main` (`git diff main...HEAD` empty; also test-enforced); entry-point convention (repo root first on `sys.path`) implemented by `verify.py:22–33` per R-037; explicit deterministic seeding (derived seed, R-052) matches the reproducibility convention.

## Smells

- **TODO/FIXME/XXX/HACK**: 0 in the namespace, the 11 v2 test files, and the helpers module.
- **Commented-out code**: none found (pattern scan over the namespace).
- **Test-only branches in production code**: none. No pytest/env-var gates in the namespace; the only `os.environ` access is the R-066 git env **denylist scrub** (V:631, a hardening measure); two `# pragma: no cover` on the `verify.py` `__main__`/package-bootstrap guards are exercised via subprocess CLI tests, not dead test hooks.
- **Assertion-free tests in the v2 suite**: **0 vacuous** of 409 test functions. An AST scan with transitive helper resolution found 18 tests with no direct `assert`; all are positive controls whose pass condition is "validator raises nothing" on a canonical fixture (`validate_profile`/`validate_record`/`validate_ledger`/`validate_suite_receipt`) — the intentional nearest-true siblings R-019 requires, not coverage gaps.

## Deviations / Adaptations (audit trail)

1. **Spec path**: the spec lives at the workflow-derived path `.correctless/specs/camera-ready-aims-evidence-2.md` (task name `camera-ready-aims-evidence-v2`; the workflow derives the `-2` filename). Content metadata names the task correctly.
2. **Review step substitution**: /creview-spec was satisfied by the external contract sign-off (`contract_freeze_signoff_2026-08-20.md`, SHA-256 pinned in the spec) plus a fidelity generator-verifier round — recorded in the pipeline manifest: "spec fidelity-verified externally (contract sign-off 2026-08-20 + generator-verifier round, recorded in lieu of /creview-spec on the contract-frozen spec)".
3. **TEST_BUG adjudications (2)**: GREEN-phase agents escalated rather than editing tests, per ABS discipline. One carries an in-tree trace: `tests/test_colm_aims_v2_schema_profile.py:633` ("TEST_BUG fix (orchestrator-adjudicated): callers pass fresh subdirs … parent must be created too" — `_stage` uses `mkdir(parents=True)`). The second adjudication is orchestrator-reported (no in-tree comment trace located); both recorded here for the audit trail.
4. **11-vs-12 citation deviation** (orchestrator-reported): a pipeline-stage citation of the test-file count deviated by one. Verified ground truth: the v2 suite is **11** `tests/test_colm_aims_v2_*.py` files plus **1** shared helpers module (`tests/_colm_aims_v2_helpers.py`) — 12 files total, 605 tests.
5. **R-052 pairing-population narrowing — RESOLVED-pending-ack** (spec Open Questions): the seed input `pairing_population_keyset_sha256` is pinned to the 2,249 complete-pair key set with **zero in-package exclusions**; the 9 upstream-unpaired items (2,258 − 2,249) are pre-package retention documentation in provenance, never in-package `excluded_keys`. Implemented (S:1100–1126) and tested (`IB::test_nonempty_in_package_excluded_keys_fails`, `IB::test_pre_package_retention_documentation_required`, triple-bind legs). Narrows the handoff §8 / v1 R-008 lineage term for the frozen v2 package only; flagged for reviewer acknowledgment at the next shuttle round; nothing blocks on it.

## Verdict

**PASS.**

- All 71 active rules implemented and tested: 69 strong, 2 adequate (R-019 distributed-corpus pairing verified manually; R-037 exit-4 path constant-pinned + negatively tested but not positively triggered), 0 weak, 0 GAP.
- Spot-runs executed and green: R-052 (16), R-059 (35), R-064 (17), R-069 (12), R-027/R-017 (8); v2 subset independently re-run: 605/0.
- No undocumented third-party or network-capable dependencies; one manifest drift to fix downstream (`requirements.txt` numpy pin 2.4.4 vs required 2.4.6).
- No architecture violations; ARCHITECTURE.md needs v2 entries added by /cupdate-arch.
- Smell scan clean (0 TODO-class markers, 0 commented-out code, 0 test-only production branches, 0 vacuous tests).
