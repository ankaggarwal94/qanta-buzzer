# qanta-buzzer Repomix Packs

Snapshot source:
- Branch: `main`
- Commit: `cda02951`

Generated with `repomix v1.4.2` in XML parsable style.

Files:
- `repomix-code.xml`: core code, configs, scripts, tests, and top-level docs needed to understand the modular implementation.
- `repomix-docs.xml`: README, CLAUDE, walkthrough, Copilot instructions, and `.planning/` state/documentation.
- `repomix-smoke.xml`: smoke-run JSON/CSV artifacts from `artifacts/smoke/`.

Suggested use:
- Use `repomix-code.xml` for implementation or code-review tasks.
- Use `repomix-docs.xml` for architecture, planning, walkthrough, and process context.
- Use `repomix-smoke.xml` only when you need the recorded smoke outputs; it is the largest and most data-heavy pack.

These files are intentionally untracked and were generated from the merged `main` branch state.
