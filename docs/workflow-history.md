# Workflow History

### 2026-08-18 — Hazard-Pretrain Efficacy Eval Harness
Branch: feature/hazard-efficacy-eval. Rules: 15. QA rounds: 2. Findings fixed: 40. Controlled A/B/C efficacy harness for the hazard bridge (E-1 --seed, E-2 per-question eval, shuffled-NLL ablation, identity-validated resume, scale-gated paired bootstrap); smoke + t5-base runs delivered the verdict in docs/hazard-efficacy-report.md (default-beta bridge significantly harmful, B==C, beta-threshold corner mechanism; Device-2 diagnostic-first recommendation).

### 2026-08-19 — Camera-Ready AIMS Evidence Flow
Branch: feature/camera-ready-aims-spec. Rules: 39. QA rounds: 2. Findings fixed: 28. Fail-closed two-mode evidence verifier (reproducibility/colm_aims_2026/) for the constructed-QA-reference sensitivity diagnostic backing the COLM 2026 AIMS paper — strict schema-versioned profile, pair/censoring count identities, release-mode per-leg binding matrix vs independently-anchored expectations, claim ledger + rights inventory, create-once receipts, CLI with pinned exit codes 0-4. Detector recommended `high`; overridden to `standard` by the author — the 17 BLOCKING + 3 CRITICAL QA findings and the 3 CRITICAL + 7 HIGH mini-audit findings support the `high` recommendation. Standard-intensity pipeline nonetheless ran the full ctdd cycle (RED→audit→GREEN→simplify→2 QA rounds→8-lens mini-audit→3 fix rounds→verify), closing every CRITICAL/HIGH release-integrity and DoS hole (self-attestation via referenced-path containment, fabricated-estimand reconciliation incl. R-025 draw substitution, unbounded-bootstrap OOM, FIFO/symlink ingress, receipt TOCTOU) before advancing.
